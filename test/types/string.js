var should = require('should'),
    dino = require('../../');

describe('string', function(){
    
    var type;
    
    beforeEach(function(){
        type = Object.create(dino.types.string);
    });
});